// Paquete del modelo: efecto del poder bestia.
package model;

// Clase concreta que representa el efecto de bestia.
public class BeastEffect implements IPowerEffect {

    // Método requerido por la interfaz de efectos.
    @Override
    // Permite ampliar este bloque con la animación o lógica de la bestia.
    public void applyEffect() {
        // El efecto se representa visualmente con la imagen de ataque.
    }
}
