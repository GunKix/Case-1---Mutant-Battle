// Paquete del modelo: efecto del poder electricidad.
package model;

// Clase concreta que representa el efecto de electricidad.
public class ElectricityEffect implements IPowerEffect {

    // Método requerido por la interfaz de efectos.
    @Override
    // Permite ampliar este bloque con la animación o lógica del rayo.
    public void applyEffect() {
        // El efecto se representa visualmente con la imagen de ataque.
    }
}
