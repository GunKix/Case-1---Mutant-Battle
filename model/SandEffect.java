// Paquete del modelo: efecto del poder arena.
package model;

// Clase concreta que representa el efecto de arena.
public class SandEffect implements IPowerEffect {

    // Método requerido por la interfaz de efectos.
    @Override
    // Permite ampliar este bloque con la animación o lógica de la arena.
    public void applyEffect() {
        // El efecto se representa visualmente con la imagen de ataque.
    }
}
