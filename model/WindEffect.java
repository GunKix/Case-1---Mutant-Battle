// Paquete del modelo: efecto del poder viento.
package model;

// Clase concreta que representa el efecto de viento.
public class WindEffect implements IPowerEffect {

    // Método requerido por la interfaz de efectos.
    @Override
    // Permite ampliar este bloque con la animación o lógica del viento.
    public void applyEffect() {
        // El efecto se representa visualmente con la imagen de ataque.
    }
}
