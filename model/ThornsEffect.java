// Paquete del modelo: efecto del poder espinas.
package model;

// Clase concreta que representa el efecto de espinas.
public class ThornsEffect implements IPowerEffect {

    // Método requerido por la interfaz de efectos.
    @Override
    // Permite ampliar este bloque con la animación o lógica de las espinas.
    public void applyEffect() {
        // El efecto se representa visualmente con la imagen de ataque.
    }
}
