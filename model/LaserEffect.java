// Paquete del modelo: efecto del poder láser.
package model;

// Clase concreta que representa el efecto de láser.
public class LaserEffect implements IPowerEffect {

    // Método requerido por la interfaz de efectos.
    @Override
    // Permite ampliar este bloque con la animación o lógica del láser.
    public void applyEffect() {
        // El efecto se representa visualmente con la imagen de ataque.
    }
}
