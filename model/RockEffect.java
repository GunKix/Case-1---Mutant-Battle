// Paquete del modelo: efecto del poder roca.
package model;

// Clase concreta que representa el efecto de roca.
public class RockEffect implements IPowerEffect {

    // Método requerido por la interfaz de efectos.
    @Override
    // Permite ampliar este bloque con la animación o lógica de la roca.
    public void applyEffect() {
        // El efecto se representa visualmente con la imagen de ataque.
    }
}
