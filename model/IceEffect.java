// Paquete del modelo: efecto del poder hielo.
package model;

// Clase concreta que representa el efecto de hielo.
public class IceEffect implements IPowerEffect {

    // Método requerido por la interfaz de efectos.
    @Override
    // Permite ampliar este bloque con la animación o lógica del hielo.
    public void applyEffect() {
        // El efecto se representa visualmente con la imagen de ataque.
    }
}
